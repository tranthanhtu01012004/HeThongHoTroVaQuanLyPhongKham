import { HasPerMissionDirective } from './has-per-mission.directive';

describe('HasPerMissionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPerMissionDirective();
    expect(directive).toBeTruthy();
  });
});
